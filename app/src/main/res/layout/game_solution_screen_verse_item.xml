<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="verse"
            type="mg.maniry.tenymana.gameLogic.models.BibleVerse" />
    </data>

    <TextView
        android:id="@+id/solutionScreenVerseItem"
        style="@style/AppTheme.VerseRowItem"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@{@string/solution_verse_item(verse.verse, verse.text)}"
        tools:text="1- In the beginning ..." />
</layout>
